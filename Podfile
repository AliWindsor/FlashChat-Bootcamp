# Use hashes as comments
platform :ios, '13.0' #define latest iOS version

target 'Flash Chat iOS13' do

  # Pods for Flash Chat iOS13
  
  pod 'CLTypingLabel'
  
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod "KeyboardAvoidingView", '~> 5.2'
  
end
